pipeline {
    agent { label params.HOSTNAME }
    parameters {
        string(name: 'HOSTNAME', defaultValue: 'default-host', description: 'Hostname to run the job on')
    }
    stages {
        stage('Hello') {
            steps {
                sh 'echo Hello, World!'
            }
        }
    }
}