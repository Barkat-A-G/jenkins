pipeline {
    agent any

    parameters {
        string(name: 'VM_IP', defaultValue: '', description: 'IP address of the Linux VM')
    }

    stages {
        stage('Hello World') {
            steps {
                sh "echo 'Hello from github'"
        }
    }
}
